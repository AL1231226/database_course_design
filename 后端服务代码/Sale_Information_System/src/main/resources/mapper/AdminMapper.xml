<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.Sale_Information_System.Mapper.AdminMapper">

    <resultMap id="BaseResultMap" type="Admin">
        <id column="admin_id" property="adminId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="admin_name" property="adminName"/>
    </resultMap>

    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT * FROM admin WHERE username = #{username}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="adminId">
        INSERT INTO admin (username, password, admin_name)
        VALUES (#{username}, #{password}, #{adminName})
    </insert>

</mapper>
